# Config examples

Portable HK Operator MCC profile JSON files for import via **Import profile**
(desktop) or copy into:

```text
~/.config/hk-operator/profiles/
```

- [`dev.json`](./dev.json) — sanitized demo (composer + URL). No private paths or 3DL-internal slash commands.

Export your live store with **Export profile** in MCC.
