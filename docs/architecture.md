# Architecture

See the root [README](../README.md) for the three-layer split (firmware / MCC / config).

Protocol notes for the hybrid GATT layout live under `mcc/protocol/` when developing from this tree.
